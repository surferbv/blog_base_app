<%= form_with(model: artilce, local: true) do |form| %>
  <% if artilce.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(artilce.errors.count, "error") %> prohibited this artilce from being saved:</h2>

      <ul>
      <% artilce.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :text %>
    <%= form.text_area:text, cols: 50, rows: 10 %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
